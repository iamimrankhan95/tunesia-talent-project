import { MongooseRepository } from '../../../../lib/mongoose-typescript/MongooseRepository';

class ClassRepository extends MongooseRepository {

}

export let classRepository = new ClassRepository();
