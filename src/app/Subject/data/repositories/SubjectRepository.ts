import { MongooseRepository } from '../../../../lib/mongoose-typescript/MongooseRepository';

class SubjectRepository extends MongooseRepository {

}

export let subjectRepository = new SubjectRepository();
